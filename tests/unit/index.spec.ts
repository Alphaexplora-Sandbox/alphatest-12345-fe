import { SERVICE_NAME } from '../../src/index';

describe('alphatest-12345-frontend', () => {
  it('should export SERVICE_NAME', () => {
    expect(SERVICE_NAME).toBe('alphatest-12345-frontend');
  });
});